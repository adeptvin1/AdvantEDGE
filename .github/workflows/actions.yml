name: GitHub Actions 
on: [push]
jobs:

  yamllint:
    name: 'Yamllint'
    runs-on: ubuntu-latest
    steps:
      - name: 'Yamllint'
        uses: nosborn/github-action-yamllint@v1
        with:
          file_or_dir: AdvantEDGE/playbooks/install-development-env.yml

